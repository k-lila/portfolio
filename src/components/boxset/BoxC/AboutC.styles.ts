import styled from 'styled-components'

const AboutC = styled.section`
  font-size: 1em;
  text-align: center;
  text-wrap: nowrap;
  margin: 0.25em 0em;
  h3 {
    letter-spacing: 1px;
    margin: 0.1em 1em;
  }

  :nth-child(1) {
    letter-spacing: 0.5px;
  }

  :nth-child(3) {
    font-style: italic;
  }
`

export default AboutC
